rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Helper function
    function isAuthenticated() {
      return request.auth != null;
    }

    // Default allow for authenticated or demo user session in web client
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
